@{
    ViewBag.Title = "Home";    
}

<div class="container">
    @if (!User.IsInRole("Admin") && !User.IsInRole("Enthusiast") && !User.IsInRole("Brewer"))
    {
        if (Request.Cookies["Over21"] == null || Request.Cookies["Over21"].Value != "true")
        {
            <div class="ageCheckerDiv">
                <div class="ageCheck">
                    <p>Are you 21?</p>
                    <div class="ageCheckerContainer">
                        <input class="ageCheckerForm" type="date" name="ageCheckerForm" autofocus />
                    </div>
                </div>
            </div>
        }}
    <div class="searchBarDiv">
        <div class="searchBarForm">
            <input autofocus class="searchBar" type="search" name="SearchString" placeholder="Search for beers or breweries" autocomplete="off" />
        </div>
        @*<div class="locationForm">
                <input class="locationBar" type="search" name="locationString" placeholder="Location" autocomplete="off" />
            </div>*@
        <div class="searchResultsPage">
            <div class="searchResults"></div>
        </div>
    </div>
</div>